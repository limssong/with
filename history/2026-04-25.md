# 2026-04-25 작업 기록

## [09:52] 배포 사이트 로그인 후 localhost 리다이렉트 원인 분석 공유

### 지시
사용자가 배포 사이트(with-running-app.vercel.app)에서 Google 로그인 후 localhost:3000/auth/callback으로 리다이렉트되는 문제 분석 내용을 공유함

### 원인
Supabase Dashboard의 Redirect URLs 허용 목록에 프로덕션 URL이 없어서, Supabase가 Site URL(localhost:3000)로 fallback

### 조치 (코드 수정 없음, 설정만)
1. Supabase Dashboard > Authentication > URL Configuration
   - Site URL → https://with-running-app.vercel.app
   - Redirect URLs 추가: https://with-running-app.vercel.app/auth/callback, http://localhost:3000/auth/callback
2. Vercel > Environment Variables: NEXT_PUBLIC_SITE_URL=https://with-running-app.vercel.app
3. Vercel Redeploy (without cache)

### 결과
사용자가 직접 설정 조치 예정 (코드 변경 없음)

## [09:56] Supabase 설정 수정 완료 — localhost 리다이렉트 문제 해결

### 수정 내용 (Supabase Dashboard만, 코드 변경 없음)
- Site URL: `http://localhost:3000` → `https://withrunning.com`
- Redirect URLs 추가:
  - `https://withrunning.com/auth/callback`
  - `http://localhost:3000/auth/callback`

### 결과
즉시 적용 (재배포 불필요). 배포 사이트 로그인 후 withrunning.com으로 정상 리다이렉트됨

## [10:39] with 프로젝트 코드 검토

### 지시
사용자: "with 프로젝트 검토해"

### 행동
1. 프로젝트 구조 파악: `/Users/limssong/claude/with-running-app/` (Next.js 16.2.2 + React 19.2.4 + TS + SCSS Modules + Supabase + Mapbox)
2. 빌드/타입/lint 검증 (병렬)
   - `npx tsc --noEmit` → 통과 (exit 0, 출력 없음)
   - `npm run lint` → 2 error / 7 warning
3. Explore 에이전트로 보안·데이터 무결성·Next 16 호환·성능·a11y 심층 검토
4. 4월 18일 작성된 `with_코드검토.md` 보고서 대비 잔여 이슈 확인 (alert/confirm, .single() 사용처)

### 결과 (요약)

**✅ 양호한 점**
- TypeScript strict 통과 (에러 0)
- `.gitignore`에 `.env*` 포함되어 시크릿 누설 위험 낮음
- Next 16 `params` Promise 처리 일관 (`use(params)`, `await params`)
- 폼 `<label htmlFor>` 연결 / `role="alert" aria-live` / `metadataBase` / robots·sitemap 모두 적용 완료
- 리스트 `key` 누락 없음, `<img>` → `next/image` 전환 완료

**⚠️ 즉시 조치 권장 (High)**
1. **ESLint 에러 2건** — `src/app/ai-demo/page.tsx:441` 따옴표 미이스케이프(`"`). `&quot;` 또는 `&ldquo;`로 교체
2. **`.single()` 위험 — 6곳** — row 없으면 throw. 다음을 `.maybeSingle()`로 교체:
   - `src/app/crews/[id]/page.tsx:74` (courses 좌표)
   - `src/app/crews/[id]/manage/page.tsx:25` (crews 호스트)
   - `src/app/auth/callback/page.tsx:18`
   - `src/app/courses/[id]/page.tsx:41` (profiles role)
   - `src/lib/auth.ts:87`, `src/lib/data.ts:117/142`
3. **`alert()`/`confirm()` 잔존** — `src/app/courses/[id]/page.tsx:50, 54` (4월 18일 보고서 P1 미적용 부분). 인라인 `role="alert"`+커스텀 모달로 교체

**⚠️ 보강 권장 (Medium)**
4. **API Route 입력 검증 부족**
   - `src/app/api/search/route.ts` — `q` 길이 제한 없음(DoS·과금 위험). `q.slice(0, 100)` 또는 zod 검증
   - `src/app/api/weather/route.ts` — `lat/lng` 숫자/범위 검증 없음, `date` 형식 검증 없음
5. **에러 응답 노출** — `/api/search` 카카오 응답을 그대로 패스스루(401/429 메시지 노출 가능). 정상 case만 매핑해 반환 권장
6. **Mapbox 번들 무거움** — `MapboxMap.tsx`에서 `mapbox-gl` 정적 임포트. 지도 미사용 페이지에도 로드됨 → `dynamic(() => import(...), { ssr: false })`

**ℹ️ 참고 (Low)**
7. ESLint 미사용 import/변수 7건 (ai-demo `useEffect`, crews/[id] `notFound`, mypage `handleSaveNickname`, lib/supabase 불필요한 disable 디렉티브 2건 등) — `npm run lint -- --fix` 일부 자동 수정 가능
8. Realtime 채널 정리 — `crews/[id]/chat/page.tsx:80`은 `removeChannel()` 사용 중. supabase-js v2에서 `removeChannel`도 동작하나 `channel.unsubscribe()`가 더 명시적

### 종합
4월 18일 보고서 P0/Sprint 1·Sprint 2까지 충실히 반영됨. **현재 빌드는 깨끗하지만 ESLint 에러 2건 + `.single()` 6곳 + alert 2곳이 잔여**. P1 후속 작업으로 처리 권장.

### 산출물
없음 (검토만 수행, 코드 미수정)

## [18:47] ai-demo 매칭 로직을 Supabase 기반 백엔드로 전환

### 지시
ai-demo 매칭 로직을 클라이언트 더미 데이터(RUNNERS 배열)에서 실제 Supabase profiles 기반으로 전환

### 행동
1. `src/app/api/match/route.ts` 신규 생성 (POST 핸들러)
   - request body: { pace, dist, time, region, currentUserId? }
   - profiles 테이블 조회 (excludeId 있으면 .neq 적용)
   - PACE_OPTIONS(6) one-hot / DIST_OPTIONS(5)·TIME_OPTIONS(5)·REGION_OPTIONS(6) multi-hot
   - 가중치 코사인 유사도: 페이스 0.35 + 거리 0.25 + 시간 0.25 + 지역 0.15
   - score > 0 결과만 내림차순 정렬, reasons는 hasOverlap 기준
   - response: { results: [{ id, name, avatar_url, pace, preferred_distance, preferred_time, preferred_region, score, reasons }] }
2. `src/app/ai-demo/page.tsx` 수정
   - 하드코딩 RUNNERS, cosineSim, condToVec, getReasons 함수 모두 제거
   - runMatching: 타이머 애니메이션(600/1300/2000/2700ms) 유지, 2700ms 시점에 POST /api/match fetch
   - 결과 카드: avatar_url 있으면 img, 없으면 name 첫 글자
   - 기존 UI(가중치 바, 처리 단계 애니메이션, 원형 점수 차트, 이유 태그) 모두 유지
3. `src/app/ai-demo/page.module.scss`
   - .resultAvatar: 원형 아바타 박스 스타일로 변경 (44px)
   - .resultAvatarImg 추가 (44px 원형 이미지)

### 결과
- `npm run build` 통과 (TypeScript 검사 포함, 경고/오류 없음)
- /api/match 가 ƒ (Dynamic) 라우트로 정상 등록
- /ai-demo 페이지가 실제 Supabase profiles 데이터로 매칭 결과 출력

## [18:51] ai-demo 매칭 코드 3건 보정 (버그/보안/타입)

### 지시
1. (버그) ai-demo fetch body에 currentUserId 누락 → 본인 프로필이 결과에 포함됨
2. (보안) route.ts 입력 배열에 .slice(0, 20) 적용해 크기 제한
3. (타입) Supabase 응답 할당을 `(data ?? []) as ProfileRow[]`로 명시적 단언

### 행동
- src/app/ai-demo/page.tsx: fetch body에 `currentUserId: null` 추가
- src/app/api/match/route.ts:
  - `body.pace.slice(0, 20)` 등 4개 배열에 slice 추가
  - `const profiles = (data ?? []) as ProfileRow[];` 로 변경

### 결과
- `npm run build` 통과 (TS 검사, 21페이지 생성 모두 정상)
- 세 수정 외 다른 코드 변경 없음

## [18:53] run/page.tsx handleVoice를 Web Speech API로 교체

### 지시
더미 setTimeout 구현을 실제 Web Speech API(SpeechRecognition) 호출로 교체

### 행동
- src/app/run/page.tsx handleVoice 함수 교체
  - window.SpeechRecognition / webkitSpeechRecognition 감지, 미지원 브라우저는 alert
  - lang='ko-KR', interimResults=false, maxAlternatives=1
  - onstart → setVoiceListening(true)
  - onresult → transcript을 🎙️ "..." 태그로 reports에 push
  - onerror → 'not-allowed' 시 마이크 권한 alert
  - onend → setVoiceListening(false)
  - HTTPS/localhost 동작 제약을 주석에 명시
  - @typescript-eslint/no-explicit-any 라인 단위 eslint-disable로 any 허용

### 결과
- 기존 UI(voiceOverlay, voiceListening 상태, 버튼 스타일) 변경 없음
- `npm run build` 통과, 다른 코드 변경 없음

## [18:58] run/page.tsx 음성 인식 3건 보정

### 지시
1. (버그) 음성 버튼 재클릭 시 recognition 중복 생성 방지 → `disabled={voiceListening}` 추가
2. (버그) 빈 transcript가 리포트에 추가되는 문제 → `trim()` + 빈 문자열 early return
3. (권장) onerror에 'audio-capture' 케이스 추가 → "마이크 장치를 찾을 수 없어요" alert

### 행동
- src/app/run/page.tsx 음성 버튼 button에 `disabled={voiceListening}` 추가
- onresult: `transcript = event.results[0][0].transcript.trim(); if (!transcript) return;`
- onerror에 `audio-capture` else if 분기 추가

### 결과
- `npm run build` 통과 (에러/경고 없음)
- 세 수정 외 다른 코드 변경 없음

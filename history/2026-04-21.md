# 2026-04-21 작업 기록

## [13:32] /courses, /crews 더미 데이터 표시 작업

### 지시
- `/courses`와 `/crews` 페이지에 더미 데이터가 화면에 표시되도록 수정
- Supabase가 비어 있어서 화면에 아무것도 안 나오는 문제 해결
- 코스 10개, 모임 10개로 확장, 기존 crews 날짜를 2026-04-21 이후로 수정

### 행동
1. **`src/lib/data.ts` 수정**
   - `localCourses`, `localCrews` import 추가 (타입도 같이 import)
   - `getCourses()`: `data.length === 0`일 때 `localCourses` 반환하도록 폴백 추가
   - `getCrews()`: `data.length === 0`일 때 `localCrews` 반환하도록 폴백 추가

2. **`src/data/courses.ts` — 6개 → 10개로 확장**
   - 기존 6개 유지
   - 추가: `namsan-loop` (남산 순환, 4.0km, medium), `gwangnaru-hangang` (광나루, 9.5km, medium), `cheonggyecheon` (청계천, 3.5km, easy), `seoul-forest` (서울숲, 5.8km, easy)

3. **`src/data/crews.ts` — 5개 → 10개로 확장**
   - 기존 5개 날짜 수정: crew-1=04-22, crew-2=04-23, crew-3=04-26, crew-4=04-24, crew-5=04-25
   - 추가: crew-6 (남산 야간 런), crew-7 (청계천 출근 전 달리기, 정기모임), crew-8 (한강 롱런 챌린지), crew-9 (서울숲 주말 피크닉런), crew-10 (광나루 한강 저녁 러닝, 매주 금요일)

4. **검증**
   - `npx tsc --noEmit`: 에러 없음
   - `npm run dev`: `/courses`, `/crews` 모두 200 응답, 런타임 에러 없음

### 결과
- TypeScript 타입 에러 없음
- `/courses` 페이지 — 코스 10개 표시 (Supabase 비어있으면 로컬 더미 반환)
- `/crews` 페이지 — 모임 10개 표시 (모든 날짜가 2026-04-22 이후 미래 날짜)
- 필터 태그 정합성 확인: `한강/공원/트레일/초급(easy)/중급(medium)/고급(hard)` 코스 필터, `초보환영/정기모임/주말/새벽/저녁` 등 모임 태그 모두 데이터에 반영됨

## [13:40] 코스 여전히 안 나옴 → Supabase 네트워크 예외 처리

### 지시
- `http://localhost:3000/courses` 코스가 여전히 안 나온다

### 원인
- `.env.local`의 Supabase 호스트(`gywlqiyvghecsdjrtiun.supabase.co`)가 DNS 실패 → supabase-js가 `{error, data}` 반환이 아니라 **Promise reject (throw)**
- 기존 폴백은 `error` 필드만 체크해서 throw 시 useEffect의 `.then(setCourses)`가 호출되지 않아 빈 배열 유지

### 행동
- `src/lib/data.ts`의 5개 함수(`getCourses`, `getCourse`, `getCrews`, `getCrew`, `getCrewsByCourse`) 모두 try/catch로 감싸고 예외 시 로컬 더미 데이터 반환
- 상세 함수들도 로컬 데이터에서 id 매칭으로 폴백

### 결과
- `npx tsc --noEmit` 통과
- `/courses`, `/crews` 200 응답, 런타임 에러 없음 — 이제 Supabase가 throw해도 로컬 더미가 안정적으로 표시됨

## [13:50] /login 버튼 무반응 수정

### 지시
- 로그인 페이지에서 카카오/Google 버튼 클릭 시 아무 반응 없음 → 에러 표시·로딩 상태 추가
- Supabase 초기화 시 env 여부 콘솔 출력 (디버그용)
- Kakao는 Supabase 공식 지원 여부 불명 → Google 우선

### 원인 분석
- 기존 `onClick={signInWithKakao}`가 **async 함수 레퍼런스**를 직접 연결 → throw된 에러가 unhandled Promise로 떠서 UI에 아무 피드백 없음
- `.env.local`의 `NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...` → 표준 JWT(eyJ...)가 아닌 신규 publishable 키 포맷. supabase-js v2 구버전은 JWT만 허용하므로 "Invalid API key" 가능성 높음

### 행동
1. **`src/app/login/page.tsx` 개편**
   - `useState`로 `loading: 'kakao' | 'google' | null`, `errorMsg` 관리
   - `handleSignIn(provider)` async 핸들러 추가: try/catch로 에러 잡아 화면에 표시
   - 로그인 중에는 버튼 `disabled` + 텍스트 "로그인 중..."
   - 에러 발생 시 빨간색 토스트 박스 렌더, console.error에도 출력
   - SVG 아이콘과 기존 스타일은 그대로 유지

2. **`src/app/login/page.module.scss`**
   - `.kakaoBtn`, `.googleBtn`에 `&:disabled { opacity: 0.6; cursor: not-allowed; }` 추가
   - `.error` 스타일 신설 (빨간 배경 + 텍스트, 가운데 정렬)

3. **`src/lib/supabase.ts` 디버그 로그**
   - 클라이언트에서만 `[supabase] init { urlPresent, keyPresent, keyPrefix, keyLooksLikeJwt }` 출력
   - 키가 `eyJ`로 시작하지 않으면 경고 메시지(대시보드에서 anon public 키 재확인 안내) 출력

### 결과
- `npx tsc --noEmit` 통과
- `/login` 200 응답, 컴파일 에러 없음
- 브라우저에서 /login 열면 DevTools Console에 `[supabase] init …` 로그 확인 가능
- 현재 `.env.local` 키 형식이 비정상(JWT 아님)이므로 버튼 클릭 시 화면에 에러 메시지 노출 예정 → 다음 단계: Supabase 대시보드 Settings → API에서 **anon public (eyJ로 시작)** 키를 복사해 `NEXT_PUBLIC_SUPABASE_ANON_KEY`에 붙여넣고 dev 서버 재시작
- Google provider / Redirect URL 설정은 키 교체 후 에러 메시지 내용에 따라 추가 조치

## [14:00] 코스/모임 상세 404 폴백 확인

### 지시
- /courses/[id] 404, /crews/[id] "불러올 수 없습니다" 문제 → 상세 조회 함수에 로컬 폴백 추가

### 행동
- **이미 직전 `/login` 작업 때 `src/lib/data.ts`의 `getCourse` / `getCrew` / `getCrewsByCourse`에 try/catch + 로컬 폴백을 적용해둔 상태**였음 (Supabase가 throw하면 `localCourses.find`, `localCrews.find`, `localCrews.filter`로 대체)
- 현재 상태 재확인 및 실제 URL 테스트:
  - `/courses/yeouido-hangang` → 200
  - `/courses/namsan-loop` → 200
  - `/crews/crew-1` → 200
  - `/crews/crew-6` → 200
- `npx tsc --noEmit` 통과

### 결과
- 상세 페이지 모두 정상 응답. 사용자 브라우저에 404가 여전히 보이면 JS 캐시 → 하드 리프레시(⌘⇧R) 필요
- 경고: `src/app/courses/` 안에 `[id]` 와 빈 `\[id\]` 두 폴더가 공존 (후자는 빈 디렉토리, 실수로 만들어진 artifact로 보임). 라우팅에 직접적 문제는 없어 보이나 제거 대상으로 추후 사용자 확인 후 삭제 예정
  - 사용자 응답: "아니야. 괜찮아." → 유지

## [14:15] 성능 개선: Supabase 타임아웃 + SSR + ISR

### 지시
- 사이트 너무 느림. `force-dynamic` 제거, ISR 적용. Supabase 2초 타임아웃. 목록 페이지 서버 컴포넌트화

### 행동
1. **`src/lib/data.ts` — 2초 타임아웃 헬퍼**
   - `withTimeout<T>(query)` 함수: `Promise.race`로 쿼리와 2초 타이머 경합, 타임아웃이면 `{ data: null, error: Error('timeout') }` 반환
   - `SUPABASE_TIMEOUT_MS = 2000` 상수
   - `getCourses`, `getCourse`, `getCrews`, `getCrew`, `getCrewsByCourse` 5개 함수 모두 withTimeout으로 감싸고 실패 시 로컬 더미 폴백 유지
   - 기존 try/catch 중첩 제거 → 깔끔해짐

2. **`src/app/page.tsx`**
   - `export const dynamic = 'force-dynamic'` → `export const revalidate = 60` 교체 (60초 ISR 캐시)

3. **`src/app/courses/page.tsx` — 서버 컴포넌트화**
   - `'use client'` 제거 → `async function CoursesPage()` 서버 컴포넌트
   - `await getCourses()`로 서버에서 데이터 패칭, HTML에 이미 카드 포함
   - 필터 인터랙션 부분은 새 파일 `src/app/courses/CoursesView.tsx`(클라이언트)로 분리, `courses` prop으로 전달
   - `revalidate = 60` 추가

4. **`src/app/crews/page.tsx` — 서버 컴포넌트화**
   - 동일 패턴: 서버에서 `await getCrews()`, 필터/AI 검색은 `src/app/crews/CrewsView.tsx`(클라이언트)로 분리
   - `revalidate = 60` 추가

### 검증
- `npx tsc --noEmit` 통과
- SSR HTML에 코스명/모임명이 직접 포함되는지 확인:
  - `/courses` HTML에 "여의도 한강", "북한산 둘레길", "남산 순환", "청계천 코스", "서울숲 코스" 포함 확인됨
  - `/crews` HTML에 "여의도 저녁", "반포 야경", "남산 야간", "청계천 출근", "광나루 한강 저녁" 포함 확인됨
- 요청 시간 측정: 콜드/웜 모두 ~2.0초 (Supabase DNS 실패 → 타임아웃 2초 + 로컬 폴백). dev 모드는 ISR 미적용이라 매번 2초. 프로덕션 빌드(`next build && next start`)에선 `revalidate=60` 덕에 첫 요청만 2초, 이후 60초 동안 캐시 HTML 즉시 반환

### 결과
- 클라이언트 useEffect 제거 → 카드 로딩 깜빡임 없음 (SSR HTML에 이미 포함)
- 2초 이내 확정적 로컬 폴백 보장
- 프로덕션 빌드에서 ISR 적용 시 실질적 응답시간 대폭 단축 예상

## [14:40] 카카오 버튼 주석 + Google 로그인 무반응 원인 규명

### 지시
1. 로그인 페이지에서 카카오 버튼/함수 주석 처리 (코드는 유지)
2. Google 로그인 무반응 원인 점검: supabase init / auth 호출 / catch 블록 확인 후 수정

### 행동
1. **`src/app/login/page.tsx`**
   - `signInWithKakao` import 주석 처리 (Google만 import)
   - `type Provider = 'google'`로 축소
   - 카카오 `<button>` JSX 블록 `{/* ... */}` 으로 주석화 → UI에서 안 보이게만 처리
   - `handleSignIn` 내부 kakao 분기 주석, google만 호출

2. **`src/lib/auth.ts` 전면 개편**
   - `resolveRedirectTo()` 헬퍼 추가: `NEXT_PUBLIC_SITE_URL` 있으면 우선 사용, 없으면 `window.location.origin`
   - `signInWithGoogle()`:
     - env 존재 여부 체크 (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`)
     - **프리플라이트**: `fetch(${url}/auth/v1/health, {apikey, timeout 3s})`로 Supabase 도달 가능 여부 확인 → 실패 시 구체적 에러 throw
     - `signInWithOAuth({skipBrowserRedirect: true})` 로 URL만 받아와서 직접 `window.location.assign`
     - 각 단계 `console.log`로 계측
   - `signInWithKakao`도 동일 패턴 적용 (나중 사용 대비)

### 원인 (근본 문제)
- `.env.local`의 `NEXT_PUBLIC_SUPABASE_URL=https://gywlqiyvghecsdjrtiun.supabase.co` 가 **DNS NXDOMAIN**: 프로젝트가 삭제/중지/URL 오타 상태
- `anon key`도 `sb_publishable_...` 포맷 (JWT 아님) → 신형 publishable 키지만 현재 supabase-js가 이 포맷을 제대로 받는지 의문
- supabase-js v2 소스 확인: `signInWithOAuth`는 `window.location.assign(url)` 하고 **에러 없이 반환** → 무반응 원인. 기존 catch 블록이 못 잡는 이유

### 검증
- `npx tsc --noEmit` 통과
- `/login` 200 응답, 카카오 버튼 HTML에서 미렌더 확인 (`kakaoBtn` 문자열 0개 매칭)
- Node에서 프리플라이트 시뮬: `fetch('https://gywlqiyvghecsdjrtiun.supabase.co/auth/v1/health') → TypeError: fetch failed (cause: getaddrinfo ENOTFOUND)`
- 예상 화면 에러:
  > Supabase 프로젝트에 연결할 수 없습니다. 프로젝트 URL(https://gywlqiyvghecsdjrtiun.supabase.co)이 유효한지, 일시중지 상태가 아닌지 확인하세요. (원인: fetch failed)

### 결과 / 사용자 조치 필요
- 코드상으로는 Google 로그인이 에러를 화면에 정확히 표시하도록 보강 완료
- **실제 로그인 복구**는 Supabase 프로젝트 URL/key 교체 없이는 불가능:
  1. https://supabase.com/dashboard 에서 프로젝트 존재/활성 여부 확인
  2. Settings → API에서 Project URL과 **anon public** 키(`eyJ...`) 복사
  3. `.env.local`의 `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` 교체
  4. `npm run dev` 재시작
  5. 그 후에도 에러 뜨면 Supabase → Authentication → Providers에서 Google 활성화 + URL Configuration에 `http://localhost:3000/auth/callback`, `https://with-ten.vercel.app/auth/callback` 추가

## [14:55] ai-demo 페이지 텍스트 교체

### 지시
1. "말로 설명하면 AI가 조건을 자동으로 뽑아줘요" → "글로 설명하면 AI가 조건을 자동으로 뽑아줘요"
2. "자연어 조건 파싱" → "자연어 검색"

### 행동
- `src/app/ai-demo/page.tsx` L207 `자연어 조건 파싱` → `자연어 검색`
- 동일 파일 L209 `말로 설명하면 ...` → `글로 설명하면 ...`

### 결과
- 텍스트 2곳만 교체, 다른 코드 변경 없음
